namespace GeometryShapes.Common;

public static class Constants
{
    /// <summary>
    /// Since operations with floating-point numbers, we use the constant of the "acceptability" of rounding
    /// </summary>
    public const double Precision = 0.001;
}